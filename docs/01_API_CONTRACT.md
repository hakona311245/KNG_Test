# KNG Fashion API Contract

This document defines the planned MVP API surface. Swagger/OpenAPI should be generated by the NestJS backend and kept consistent with this contract.

## API Conventions

### Base Path

```text
/api
```

### Auth

JWT authentication uses HttpOnly cookies:

- `access_token`: short-lived JWT.
- `refresh_token`: rotating refresh token.

Tokens must not be stored in localStorage. Refresh-token rotation is required. Refresh tokens or refresh sessions must be stored hashed in the database.

### Roles

- `CUSTOMER`
- `ADMIN`

### Customer Statuses

- `ACTIVE`
- `BLOCKED`
- `INACTIVE`

Blocked or inactive customers cannot log in or place orders.

### Product Types

- `SHIRT`
- `PANT`
- `JACKET`

Product types are fixed for MVP. There is no product type CRUD API in the first version.

### Sizes

- `S`
- `M`
- `L`
- `XL`

Sizes are fixed for MVP. There is no size CRUD API in the first version.

### Colors

Colors are free-text values stored on product variants.

### Payment Options

- `COD`
- `VNPAY`

Both payment options route through mock payment behavior for MVP. Mock payment always succeeds.

### Order Statuses

- `PENDING`
- `CONFIRMED`
- `PROCESSING`
- `SHIPPED`
- `DELIVERED`
- `CANCELLED`

### Common Response Shape

```json
{
  "data": {},
  "message": "Success"
}
```

### Common Error Shape

```json
{
  "statusCode": 400,
  "message": "Validation failed",
  "error": "Bad Request"
}
```

## User Service

The User service owns authentication, user identity, customer account status, roles, and refresh sessions.

Total MVP endpoints: 8

### Endpoint Overview

```http
POST /api/auth/register
POST /api/auth/login
POST /api/auth/refresh
POST /api/auth/logout
GET /api/auth/me
GET /api/admin/customers
GET /api/admin/customers/:id
PATCH /api/admin/customers/:id/status
```

### Register Customer

```http
POST /api/auth/register
```

Request:

```json
{
  "email": "customer@example.com",
  "fullName": "Nguyen Van A",
  "password": "StrongPassword123",
  "phoneNumber": "0900000000"
}
```

Response:

```json
{
  "data": {
    "id": "user-id",
    "email": "customer@example.com",
    "fullName": "Nguyen Van A",
    "phoneNumber": "0900000000",
    "role": "CUSTOMER",
    "status": "ACTIVE"
  },
  "message": "Customer registered successfully"
}
```

### Login

```http
POST /api/auth/login
```

Request:

```json
{
  "email": "customer@example.com",
  "password": "StrongPassword123"
}
```

Response:

```json
{
  "data": {
    "user": {
      "id": "user-id",
      "email": "customer@example.com",
      "fullName": "Nguyen Van A",
      "phoneNumber": "0900000000",
      "role": "CUSTOMER",
      "status": "ACTIVE"
    }
  },
  "message": "Logged in successfully"
}
```

Set-Cookie:

```text
access_token=...; HttpOnly; Path=/; SameSite=Lax
refresh_token=...; HttpOnly; Path=/api/auth; SameSite=Lax
```

Blocked or inactive users must receive an authentication error and no auth cookies.

Auth error messages used by the current backend:

- Missing access cookie: `Authentication is required.`
- Invalid or expired access token: `Invalid or expired access token.`
- Missing refresh cookie: `Refresh token is required.`
- Invalid or expired refresh token: `Invalid or expired refresh token.`
- Invalid refresh session: `Refresh session is invalid.`
- Blocked or inactive account: `Account is not active.`
- Non-admin user on admin endpoint: `You do not have permission to access this resource.`

### Refresh Token

```http
POST /api/auth/refresh
```

Behavior:

- Validates the current refresh token from HttpOnly cookie.
- Validates the user is active.
- Rotates refresh token.
- Stores only the hashed refresh token/session value in database.
- Issues new access and refresh cookies.

Response:

```json
{
  "data": {
    "user": {
      "id": "user-id",
      "email": "customer@example.com",
      "fullName": "Nguyen Van A",
      "phoneNumber": "0900000000",
      "role": "CUSTOMER",
      "status": "ACTIVE"
    }
  },
  "message": "Session refreshed successfully"
}
```

### Logout

```http
POST /api/auth/logout
```

Behavior:

- Invalidates the current refresh session.
- Clears auth cookies.

Response:

```json
{
  "data": null,
  "message": "Logged out successfully"
}
```

### Current User

```http
GET /api/auth/me
```

Auth: `CUSTOMER` or `ADMIN`

Response:

```json
{
  "data": {
    "id": "user-id",
    "email": "customer@example.com",
    "fullName": "Nguyen Van A",
    "phoneNumber": "0900000000",
    "role": "CUSTOMER",
    "status": "ACTIVE"
  },
  "message": "Current user retrieved successfully"
}
```

### List Customers

```http
GET /api/admin/customers
```

Auth: `ADMIN`

Query parameters:

- `status`: `ACTIVE`, `BLOCKED`, `INACTIVE`
- `page`: defaults to `1`
- `limit`: defaults to `20`

Response:

```json
{
  "data": {
    "items": [
      {
        "id": "user-id",
        "email": "customer@example.com",
        "fullName": "Nguyen Van A",
        "phoneNumber": "0900000000",
        "role": "CUSTOMER",
        "status": "ACTIVE"
      }
    ],
    "page": 1,
    "limit": 20,
    "total": 1
  },
  "message": "Customers retrieved successfully"
}
```

### Get Customer Detail

```http
GET /api/admin/customers/:id
```

Auth: `ADMIN`

Response:

```json
{
  "data": {
    "id": "user-id",
    "email": "customer@example.com",
    "fullName": "Nguyen Van A",
    "phoneNumber": "0900000000",
    "role": "CUSTOMER",
    "status": "ACTIVE"
  },
  "message": "Customer retrieved successfully"
}
```

### Update Customer Status

```http
PATCH /api/admin/customers/:id/status
```

Auth: `ADMIN`

Request:

```json
{
  "status": "BLOCKED"
}
```

Customers are not permanently deleted in the MVP.

Response:

```json
{
  "data": {
    "id": "user-id",
    "email": "customer@example.com",
    "fullName": "Nguyen Van A",
    "phoneNumber": "0900000000",
    "role": "CUSTOMER",
    "status": "BLOCKED"
  },
  "message": "Customer status updated successfully"
}
```

## Product Service

The Product service owns product catalog reads, admin product management, fixed product type options, fixed size options, free-text variant colors, product and variant images, variant stock, and product soft delete.

Total MVP endpoints: 12

### Endpoint Overview

```http
GET /api/products
GET /api/products/:id
GET /api/products/options
GET /api/admin/products
POST /api/admin/products
GET /api/admin/products/:id
PATCH /api/admin/products/:id
DELETE /api/admin/products/:id
GET /api/admin/products/:productId/variants
POST /api/admin/products/:productId/variants
PATCH /api/admin/variants/:variantId
DELETE /api/admin/variants/:variantId
```

### List Products

```http
GET /api/products
```

Query parameters:

- `type`: `SHIRT`, `PANT`, `JACKET`
- `size`: `S`, `M`, `L`, `XL`
- `color`: free-text color name
- `page`
- `limit`

Response:

```json
{
  "data": {
    "items": [
      {
        "id": "product-id",
        "name": "Essential Shirt",
        "type": "SHIRT",
        "material": "Cotton",
        "price": 250000,
        "thumbnailUrl": "https://example.com/product-front.jpg",
        "availableColors": ["Black", "White"],
        "availableSizes": ["S", "M", "L"],
        "isActive": true
      }
    ],
    "page": 1,
    "limit": 12,
    "total": 1
  },
  "message": "Products retrieved successfully"
}
```

Only active, non-deleted products should be returned to customers.

### Get Product Detail

```http
GET /api/products/:id
```

Response:

```json
{
  "data": {
    "id": "product-id",
    "name": "Essential Shirt",
    "description": "Daily cotton shirt",
    "type": "SHIRT",
    "material": "Cotton",
    "price": 250000,
    "thumbnailUrl": "https://example.com/product-front.jpg",
    "images": [
      {
        "id": "image-id",
        "url": "https://example.com/product-front.jpg",
        "altText": "Front view",
        "sortOrder": 1,
        "isPrimary": true
      }
    ],
    "isActive": true,
    "variants": [
      {
        "id": "variant-id",
        "size": "M",
        "color": "Black",
        "stock": 10,
        "isActive": true,
        "images": [
          {
            "id": "variant-image-id",
            "url": "https://example.com/product-black-m.jpg",
            "altText": "Black size M",
            "sortOrder": 1,
            "isPrimary": true
          }
        ]
      }
    ]
  },
  "message": "Product retrieved successfully"
}
```

Product price is stored on the product. All variants of the same product use the same product price.

Product images are stored as URL strings. Product detail returns product-level images and variant-level images. `thumbnailUrl` is selected from the primary product image, falling back to the first product image by `sortOrder`.

### Get Product Options

```http
GET /api/products/options
```

Response:

```json
{
  "data": {
    "types": ["SHIRT", "PANT", "JACKET"],
    "sizes": ["S", "M", "L", "XL"]
  },
  "message": "Product options retrieved successfully"
}
```

Colors are intentionally not returned as managed options because colors are free text on variants.

### List Admin Products

```http
GET /api/admin/products
```

Auth: `ADMIN`

Query parameters:

- `type`: `SHIRT`, `PANT`, `JACKET`
- `includeDeleted`: boolean
- `page`
- `limit`

### Create Product

```http
POST /api/admin/products
```

Auth: `ADMIN`

Request:

```json
{
  "name": "Essential Shirt",
  "description": "Daily cotton shirt",
  "type": "SHIRT",
  "material": "Cotton",
  "price": 250000,
  "images": [
    {
      "url": "https://example.com/product-front.jpg",
      "altText": "Front view",
      "sortOrder": 1,
      "isPrimary": true
    }
  ]
}
```

Product create requires at least one product-level image.

### Get Admin Product Detail

```http
GET /api/admin/products/:id
```

Auth: `ADMIN`

### Update Product

```http
PATCH /api/admin/products/:id
```

Auth: `ADMIN`

Request:

```json
{
  "name": "Updated Essential Shirt",
  "description": "Updated description",
  "type": "SHIRT",
  "material": "Cotton",
  "price": 260000,
  "images": [
    {
      "url": "https://example.com/product-front-updated.jpg",
      "altText": "Updated front view",
      "sortOrder": 1,
      "isPrimary": true
    }
  ],
  "isActive": true
}
```

If `images` is provided during update, it replaces the product's current non-deleted product-level images.

### Soft Delete Product

```http
DELETE /api/admin/products/:id
```

Auth: `ADMIN`

Behavior: soft delete only.

### List Product Variants

```http
GET /api/admin/products/:productId/variants
```

Auth: `ADMIN`

### Create Product Variant

```http
POST /api/admin/products/:productId/variants
```

Auth: `ADMIN`

Request:

```json
{
  "size": "M",
  "color": "Black",
  "stock": 10,
  "images": [
    {
      "url": "https://example.com/product-black-m.jpg",
      "altText": "Black size M",
      "sortOrder": 1,
      "isPrimary": true
    }
  ]
}
```

Rules:

- `size` must be one of `S`, `M`, `L`, `XL`.
- `color` is free text.
- The same product cannot have duplicate active variants with the same size and color.
- Variant images are optional.

### Update Product Variant

```http
PATCH /api/admin/variants/:variantId
```

Auth: `ADMIN`

Request:

```json
{
  "size": "L",
  "color": "Black",
  "stock": 12,
  "images": [
    {
      "url": "https://example.com/product-black-l.jpg",
      "altText": "Black size L",
      "sortOrder": 1,
      "isPrimary": true
    }
  ],
  "isActive": true
}
```

If `images` is provided during update, it replaces the variant's current non-deleted variant-level images.

### Soft Delete Product Variant

```http
DELETE /api/admin/variants/:variantId
```

Auth: `ADMIN`

Behavior: soft delete only.

## CartAndOrder Service

The CartAndOrder service owns customer carts, checkout, mock payment, stock reduction during order placement, order history, cancellation requests, and admin order management.

Total MVP endpoints: 12

### Endpoint Overview

```http
GET /api/cart
POST /api/cart/items
PATCH /api/cart/items/:itemId
DELETE /api/cart/items/:itemId
POST /api/checkout
GET /api/orders
GET /api/orders/:id
POST /api/orders/:id/cancel-request
GET /api/admin/orders
GET /api/admin/orders/:id
PATCH /api/admin/orders/:id/status
POST /api/admin/orders/:id/cancel
```

### Get Cart

```http
GET /api/cart
```

Auth: `CUSTOMER`

Response:

```json
{
  "data": {
    "id": "cart-id",
    "items": [
      {
        "id": "cart-item-id",
        "variantId": "variant-id",
        "productId": "product-id",
        "productName": "Essential Shirt",
        "size": "M",
        "color": "Black",
        "price": 250000,
        "quantity": 2,
        "subtotal": 500000
      }
    ],
    "total": 500000
  },
  "message": "Cart retrieved successfully"
}
```

Cart is stored in the database for logged-in customers.

### Add Item To Cart

```http
POST /api/cart/items
```

Auth: `CUSTOMER`

Request:

```json
{
  "variantId": "variant-id",
  "quantity": 2
}
```

### Update Cart Item

```http
PATCH /api/cart/items/:itemId
```

Auth: `CUSTOMER`

Request:

```json
{
  "quantity": 3
}
```

### Remove Cart Item

```http
DELETE /api/cart/items/:itemId
```

Auth: `CUSTOMER`

### Checkout

```http
POST /api/checkout
```

Auth: `CUSTOMER`

Request:

```json
{
  "shippingName": "Nguyen Van A",
  "phone": "0900000000",
  "address": "123 Nguyen Trai",
  "city": "Ho Chi Minh City",
  "note": "Call before delivery",
  "paymentOption": "COD"
}
```

Behavior:

- Validates customer account status is `ACTIVE`.
- Validates cart ownership.
- Validates variant stock.
- Runs mock payment.
- Mock payment always succeeds.
- Creates order and order items.
- Stores price snapshot on each order item.
- Reduces stock after successful mock payment.
- Clears cart.

Response:

```json
{
  "data": {
    "orderId": "order-id",
    "status": "PENDING",
    "paymentOption": "COD",
    "paymentStatus": "PAID",
    "total": 500000
  },
  "message": "Order placed successfully"
}
```

### List My Orders

```http
GET /api/orders
```

Auth: `CUSTOMER`

### Get My Order Detail

```http
GET /api/orders/:id
```

Auth: `CUSTOMER`

Response includes order item price snapshots:

```json
{
  "data": {
    "id": "order-id",
    "status": "PENDING",
    "paymentOption": "COD",
    "paymentStatus": "PAID",
    "shippingName": "Nguyen Van A",
    "phone": "0900000000",
    "address": "123 Nguyen Trai",
    "city": "Ho Chi Minh City",
    "note": "Call before delivery",
    "items": [
      {
        "productId": "product-id",
        "variantId": "variant-id",
        "productName": "Essential Shirt",
        "size": "M",
        "color": "Black",
        "unitPrice": 250000,
        "quantity": 2,
        "subtotal": 500000
      }
    ],
    "total": 500000
  },
  "message": "Order retrieved successfully"
}
```

### Request Order Cancellation

```http
POST /api/orders/:id/cancel-request
```

Auth: `CUSTOMER`

Allowed when order status is `PENDING`.

Request:

```json
{
  "reason": "I selected the wrong size"
}
```

### List Admin Orders

```http
GET /api/admin/orders
```

Auth: `ADMIN`

Query parameters:

- `status`
- `page`
- `limit`

### Get Admin Order Detail

```http
GET /api/admin/orders/:id
```

Auth: `ADMIN`

### Update Order Status

```http
PATCH /api/admin/orders/:id/status
```

Auth: `ADMIN`

Request:

```json
{
  "status": "CONFIRMED"
}
```

### Cancel Order

```http
POST /api/admin/orders/:id/cancel
```

Auth: `ADMIN`

Request:

```json
{
  "adminNote": "Approved customer cancellation request"
}
```

## Swagger

The NestJS API must expose Swagger documentation for local development.

Planned path:

```text
/api/docs
```
